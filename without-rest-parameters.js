/**
 * Membuat fungsi sum
 * Menjumlahkan semua bilangan
 * Fungsi memiliki 2 parameter
 
function sum (a, b) {
    const hasil = a + b;
    return hasil;
}
sum(1,2);
*/

function sum (a, b, c, d, e) {
    const hasil = a + b + c + d + e;
    return hasil;
}
sum(1,2,3,4,5);